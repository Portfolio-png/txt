// Byte-identical duplicate of the core_erp model until now, which meant the
// two PenPaperBaseline types were structurally identical but not assignable to
// each other. Re-exported so there is one type behind both import paths.
export 'package:core_erp/features/production_pipelines/domain/pen_paper_baseline.dart';
